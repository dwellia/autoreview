{
  "name": "dwellia-vrbo-review",
  "version": "1.0.0",
  "description": "Auto-generates VRBO guest review drafts and texts them to the owner on checkout",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "test:webhook": "node scripts/test-webhook.js"
  },
  "dependencies": {}
}
